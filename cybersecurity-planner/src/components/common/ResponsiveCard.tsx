import React from 'react'; interface ResponsiveCardProps { title: string; selected: boolean; onClick: () => void; } const ResponsiveCard = ({ title, selected, onClick }: ResponsiveCardProps) => { return (<div onClick={onClick} style={{ padding: '20px', margin: '10px', border: selected ? '3px solid limegreen' : '1px solid gray', backgroundColor: selected ? '#e0ffe0' : 'white', borderRadius: '8px', cursor: 'pointer', transition: 'all 0.3s ease-in-out', boxShadow: selected ? '0px 0px 10px limegreen' : '0px 0px 5px gray', textAlign: 'center', fontWeight: 'bold' }}>{title}</div>); }; export default ResponsiveCard;
