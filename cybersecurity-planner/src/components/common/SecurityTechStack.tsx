import React from 'react'; import { useDispatch, useSelector } from 'react-redux'; import ResponsiveCard from './ResponsiveCard'; import { toggleMitreBox } from '@/state/mitre/mitre.actions'; const SecurityTechStack = () => { const dispatch = useDispatch(); const mitreBoxes = useSelector((state: any) => state.mitre.mitreBoxes); const handleToggle = (id: number) => { dispatch(toggleMitreBox(id)); dispatch({ type: mitreBoxes.find((box: any) => box.id === id)?.selected ? 'DECREASE_PROGRESS' : 'INCREASE_PROGRESS', payload: 10 }); }; return <> {mitreBoxes.map((item: any) => (<ResponsiveCard key={item.id} title={item.name} selected={item.selected} onClick={() => handleToggle(item.id)} />))} </>; }; export default SecurityTechStack;
